import { IS9ProjectSession } from '../../../develop/s9-project.interfaces';
import { IFixtureData } from '../../fixture.interface';

export const s9ProjectSessions: IFixtureData<IS9ProjectSession> = {
  data: [
  ],
  options: {
    indices: ['id', 'ownerId'],
  },
};
