elastic {
  user = "elastic"
  password = "changeme"
  host = "localhost"
  port = 9200
  job_index = "cortex_jobs_dev"
  job_type = "job"
  heartbeat_index = "cortex_job_heartbeats_dev"
  heartbeat_type = "heartbeat"
  useAuth = false
  useSSL = false
  timeout = 60
}
